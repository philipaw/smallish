# Migration `20190926003633-group-chats`

This migration has been generated by philipnb at 9/26/2019, 12:36:33 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "smallish-prisma2"."Group" (
  "id" text NOT NULL  ,
  "name" text NOT NULL DEFAULT '' ,
  PRIMARY KEY ("id")
);

CREATE TABLE "smallish-prisma2"."Chat" (
  "content" text NOT NULL DEFAULT '' ,
  "createdAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  "id" text NOT NULL  ,
  "sender" text   REFERENCES "smallish-prisma2"."User"("id") ON DELETE SET NULL,
  "updatedAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  PRIMARY KEY ("id")
);

CREATE TABLE "smallish-prisma2"."_UserGroupAdmin" (
  "A" text   REFERENCES "smallish-prisma2"."Group"("id") ON DELETE CASCADE,
  "B" text   REFERENCES "smallish-prisma2"."User"("id") ON DELETE CASCADE
);

CREATE TABLE "smallish-prisma2"."_UserGroupMember" (
  "A" text   REFERENCES "smallish-prisma2"."Group"("id") ON DELETE CASCADE,
  "B" text   REFERENCES "smallish-prisma2"."User"("id") ON DELETE CASCADE
);

ALTER TABLE "smallish-prisma2"."Chat" ADD COLUMN "group" text   REFERENCES "smallish-prisma2"."Group"("id") ON DELETE SET NULL;

CREATE UNIQUE INDEX "_UserGroupAdmin_AB_unique" ON "smallish-prisma2"."_UserGroupAdmin"("A","B")

CREATE UNIQUE INDEX "_UserGroupMember_AB_unique" ON "smallish-prisma2"."_UserGroupMember"("A","B")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20190924143724-post-author-required..20190926003633-group-chats
--- datamodel.dml
+++ datamodel.dml
@@ -7,9 +7,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url      = "postgresql://postgres@localhost:32770/smallish?schema=smallish-prisma2"
+  url      = "postgresql://postgres@localhost:32769/smallish?schema=smallish-prisma2"
 }
 model Post {
   id        String   @default(cuid()) @id
@@ -21,10 +21,29 @@
   author    User
 }
 model User {
-  id       String  @default(cuid()) @id
-  email    String  @unique
-  password String
-  name     String?
-  posts    Post[]
+  id              String  @default(cuid()) @id
+  email           String  @unique
+  password        String
+  name            String?
+  posts           Post[]
+  groupsAdminFor  Group[] @relation(name:"UserGroupAdmin")
+  groups          Group[] @relation(name:"UserGroupMember")
+}
+
+model Group {
+  id      String @default(cuid()) @id
+  name    String
+  admins  User[] @relation(name:"UserGroupAdmin")
+  members User[] @relation(name:"UserGroupMember")
+  chats   Chat[]
+}
+
+model Chat {
+  id        String @default(cuid()) @id
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  content   String
+  sender    User
+  group     Group
 }
```

## Photon Usage

You can use a specific Photon built for this migration (20190926003633-group-chats)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20190926003633-group-chats'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
